<h1>Clientes</h1>
<div class="table-responsive">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Rut</th>
      <th>Dv</th>
      <th>Nombre </th>
      <th>Apellido</th>
      <th>Institución</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.rut %></td>
        <td><%= client.dv %></td>
        <td><%= client.name %></td>
        <td><%= client.last_name %></td>
        <td><%= client.institution_id %></td>
        <td><button type="button" class="btn btn-success"><%= link_to 'Show', client %></button>
        <button type="button" class="btn btn-warning"><%= link_to 'Edit', edit_client_path(client) %></button>
        <button type="button" class="btn btn-danger"><%= link_to 'Destroy', client, method: :delete, data: { confirm: 'Está seguro(a) que quiere eliminarlo?' } %></button></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>


<br>

<%= link_to 'New Client', new_client_path %>
