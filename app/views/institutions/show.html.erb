<div class="table-responsive">
<table class="table table-striped">
  <thead>
  	<tr>
  		<th>Rut</th>
  		<th>Institución</th>
  	</tr>
  </thead>
  <tbody>
  	<tr>
  		 <td><%= number_to_currency(@institution.rut, {:precision => 0, :unit => "",:delimiter => "."}) %>-<%= @institution.dv %></td>
  		<td><%= @institution.business_name %></td>
  	</tr>
  </tbody>
 </table>
 </div>

<%= link_to 'Editar', edit_institution_path(@institution) %> |
<%= link_to 'Volver', institutions_path %>